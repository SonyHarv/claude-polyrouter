---
name: config
description: Show active configuration (global + project merged)
user_invokable: true
---

# /config Command

Display the active merged configuration.

## Usage

```
/polyrouter:config
```

## Output

Shows the effective configuration after merging defaults, global, and project overrides. Indicates which config files were found and applied.
